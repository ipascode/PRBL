<p id="notice"><%= notice %></p>

<h1>Bus Models</h1>

<%= link_to 'New Bus Model', new_bus_model_path, :class => "btn btn-default" %>
<%= link_to 'Back', buses_path, :class => "btn btn-default" %>
<h3>Import a CSV File</h3>
  <%= form_tag import_bus_models_path, multipart: true do %>
    <%= file_field_tag :file %>
    <%= submit_tag "Import CSV" %>
  <% end %>
</div>
<div class="table-responsive">
<table class="table">
  <thead>
    <tr>
      <th>Brand</th>
      <th colspan="3">Actions</th>
    </tr>
  </thead>

  <tbody class="table-hover">
    <% @bus_models.each do |bus_model| %>
      <tr>
        <td><%= bus_model.brand %></td>
        <td><%= link_to 'View', bus_model, :class => "btn btn-default" %></td>
        <td><%= link_to 'Edit', edit_bus_model_path(bus_model), :class => "btn btn-default" %></td>
        <td><%= link_to 'Delete', bus_model, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-danger" %></td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>

<br>


